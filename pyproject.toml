[build-system]
requires = ["maturin>=1.9.4,<2.0"]
build-backend = "maturin"

[project]
name = "libwsrx"
dynamic = ["version"]
requires-python = ">=3.9"
readme = "README.md"

[tool.maturin]
bindings = "pyo3"
features = ["python"]
